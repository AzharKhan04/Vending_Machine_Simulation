const CurrencyListService = () => {
    // return new Promise((resolve, reject) => {
    //     if (this.products) {
    //         resolve(this.products)
    //     } else {
    //         reject({
    //             error: 'no products found'
    //         })
    //     }
    // })

}